// Stub route so expo-router registers the "Más" tab. The tab press is always
// intercepted in the layout (opens the MoreSheet), so this never renders.
export default function MoreStub() {
  return null
}
